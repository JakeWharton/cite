rootProject.name = 'build-logic'

include(':gradle-plugin')
project(':gradle-plugin').projectDir = new File('../cite-gradle-plugin')

dependencyResolutionManagement {
	versionCatalogs {
		libs {
			from(files('../gradle/libs.versions.toml'))
		}
	}
}
